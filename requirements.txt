# SCADA Lead-Time Benchmark — pinned dependencies (action #11/#12, reproducibility).
#
# Versions below are pinned (==) to the exact set the committed results were produced
# with (Python 3.13, win-amd64). For a byte-for-byte environment use requirements.lock.txt
# (full `pip freeze`). PyTorch is OPTIONAL — only needed for deep_svdd / lstm_ae; install
# the CPU wheel separately (see note). Everything else runs CPU-only.

# ─── Core scientific stack ────────────────────────────────────────────────────
numpy==2.4.6
pandas==3.0.3
scipy==1.17.1

# ─── Machine learning ─────────────────────────────────────────────────────────
scikit-learn==1.9.0
joblib==1.5.3

# ─── Explainability ───────────────────────────────────────────────────────────
shap==0.51.0

# ─── Visualization ────────────────────────────────────────────────────────────
matplotlib==3.10.9
seaborn==0.13.2
plotly==5.22.0

# ─── Data I/O ─────────────────────────────────────────────────────────────────
pyarrow==24.0.0

# ─── Changepoint detection (onset PELT option) ────────────────────────────────
ruptures==1.1.10

# ─── Wavelet denoiser (Phase 2.5 mechanism test, src/robustness.py) ───────────
# Optional: the wavelet denoiser is skipped with a logged note if absent; the
# noise sweep and median/MA/Kalman denoisers run without it.
PyWavelets==1.8.0

# ─── Utilities ────────────────────────────────────────────────────────────────
tqdm==4.67.3

# ─── Tests ────────────────────────────────────────────────────────────────────
pytest==9.1.0

# ─── Notebooks (ONGC dashboard only) ──────────────────────────────────────────
jupyter>=1.0
ipykernel>=6.0

# ─── Deep learning (OPTIONAL) ─────────────────────────────────────────────────
# Required only for the deep detectors (deep_svdd / lstm_ae / tcn / transformer_ad).
# Install the CPU build separately so the index URL stays out of the resolver:
#   pip install torch==2.12.0 --index-url https://download.pytorch.org/whl/cpu
# torch==2.12.0

# Conformal prediction is implemented natively in src/uncertainty.py — no extra package.
