# src/loaders/ — optional external dataset loaders that register into src.datasets.
# Importing this package is a no-op; individual loader modules self-register via
# @register_loader when imported (see src/datasets.py bottom import hook).
